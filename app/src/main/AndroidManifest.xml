<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jer.paintballApp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.jer.paintballApp.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.jer.paintballApp.activities.RegisterActivity" />
        <activity android:name="com.jer.paintballApp.activities.UserAreaActivity"></activity>
        <activity android:name="com.jer.paintballApp.activities.ShowAllOrdersActivity"></activity>
        <activity android:name="com.jer.paintballApp.activities.ShowSingleRecordActivity"></activity>
        <activity android:name="com.jer.paintballApp.Pop" android:theme="@style/AppTheme.PopTheme"></activity>
    </application>

</manifest>
